//A área do retangulo é base x altura, a função ira receber os 2 valores, multiplicá-los.

function areaRetangulo(base, altura) {
  
  return base * altura;
}
console.log (areaRetangulo(4, 8));

module.exports = areaRetangulo;